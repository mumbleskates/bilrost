[package]
name = "derived-schema-test"
version = "0.0.0"
publish = false
edition.workspace = true

[dependencies]
bilrost = { path = ".." }
test-types = { path = "../test-types" }

[features]
test-suite-extra = ["bilrost/threadsafe-schema"]
