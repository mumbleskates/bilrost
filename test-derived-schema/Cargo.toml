[package]
name = "test-derived-schema"
version = "0.0.0"
publish = false
edition.workspace = true

[dependencies]
bilrost = { path = ".." }
test-types = { path = "../test-types" }

[features]
test-suite-extra = []
