[package]
name = "test-types"
version = "0.0.0"
publish = false
edition.workspace = true

[dependencies]
bilrost = { path = "..", default-features = false, features = [
    "auto-optimize",
    "bstr",
    "bytestring",
    "chrono",
    "derive",
    "no-recursion-limit",
    "smol_str",
    "std",
    "time",
    "tinyvec",
] }
bilrost-types = { path = "../bilrost-types" }
bstr = "1.10"
bytes = "1"
bytestring = "1.3.1"
chrono = "0.4.34"
jiff = { version = "0.2.18", optional = true }
smol_str = { version = ">=0.1.24,<0.4", default-features = false }
time = "0.3.22"
tinyvec = "1"

[features]
full-type-support = [
    "jiff",
]
jiff = [
    "dep:jiff",
    "bilrost/jiff",
]
test-suite-extra = []
